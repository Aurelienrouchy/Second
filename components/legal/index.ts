/**
 * components/legal barrel — shared, presentational legal document bodies.
 *
 * Consumed by both the authenticated settings routes (app/settings/*) and the
 * public consent-time routes (app/legal/*) so the legal copy lives in one place.
 */
export { TermsContent } from './TermsContent';
export { PrivacyPolicyContent } from './PrivacyPolicyContent';
